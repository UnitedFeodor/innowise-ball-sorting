package enums;

import java.util.Comparator;

public enum BallAttribute implements Comparator {
    CLASS{
        @Override
        public int compare(Object o1, Object o2) { // TODO implement and think about generics
            return 0;
        }
    }, WEIGHT{
        @Override
        public int compare(Object o1, Object o2) {
            return 0;
        }
    }, DIAMETER{
        @Override
        public int compare(Object o1, Object o2) {
            return 0;
        }
    }, COLOR {
        @Override
        public int compare(Object o1, Object o2) {
            return 0;
        }
    }


}
